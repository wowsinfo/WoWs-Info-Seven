import 'package:flutter/material.dart';

/// PlayerRankShipInfoPage class
class PlayerRankShipInfoPage extends StatelessWidget {
  PlayerRankShipInfoPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('PlayerRankShipInfoPage')
      ),
      body: Container(),
    );
  }
}
