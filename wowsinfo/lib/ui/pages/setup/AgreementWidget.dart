import 'package:flutter/material.dart';

/// AgreementWidget class
class AgreementWidget extends StatelessWidget {
  AgreementWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('AgreementWidget')
      ),
      body: Container(),
    );
  }
}
